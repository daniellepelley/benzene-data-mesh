# Platform & Cross-Cutting — Component Design

> **Status:** Not started. Scope defined in
> [top-level design §5.7](../00-top-level-design.md). To be produced in its own
> design session.

**Scope:** Benzene composition conventions (handlers, middleware for authn/authz/
metering/logging), hosting adapters and deployment topology options, configuration
store, internal eventing, observability.

**Depends on:** —
