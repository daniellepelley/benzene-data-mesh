# Consumer Layer (Ingestion) — Component Design

> **Status:** Not started. Scope defined in
> [top-level design §5.1](../00-top-level-design.md). To be produced in its own
> design session.

**Scope:** connector port design (`IDataSourceConnector`), source configuration
schema, credential/vault handling, schema discovery & catalog registration, and the
initial connectors: Databricks, SQL databases, Excel.

**Depends on:** platform.md
